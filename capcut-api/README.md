# CapCut API (server)

HTTP API service. Full docs: [../README.md](../README.md)

```bash
npm install
npm start
# http://localhost:7000
```

Core: `POST /login` · `GET /templates` · `POST /render` · `GET /render/status/:jobId`
